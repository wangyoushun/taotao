<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.taotao.manage.mapper.IUserMapper">
	<resultMap type="com.taotao.manage.pojo.User" id="userMap" autoMapping="true">
	</resultMap>


	<select id="queryUsers" resultMap="userMap">
		SELECT * FROM user
	</select>

	
</mapper>